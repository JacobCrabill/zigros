# Typesupport Library for FastRTPS

This provides the Python libraries (rosidl generator extensions) and compiled libraries for the
FastRTPS typesupport used by rmw_fastrtps_cpp and rmw_zenoh_cpp.

The Python libraries are used to generate C++ code which implements the serialization and
deserialization of ROS interfaces using FastCDR.
